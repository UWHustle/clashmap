extern crate omap;

#[cfg(test)]
mod tests {
    use omap::ConcurrentOrderedHashMap;

//    #[test]
//    fn len() {
//        let mut a = ConcurrentOrderedHashMap::new();
//        assert_eq!(a.len(), 0);
//        a.insert_back(1, "a");
//        assert_eq!(a.len(), 1);
//    }

//    #[test]
//    fn is_empty() {
//        let mut a = ConcurrentOrderedHashMap::new();
//        assert!(a.is_empty());
//        a.insert_back(1, "a");
//        assert!(!a.is_empty());
//    }
//
//    #[test]
//    fn get() {
//        let mut map = ConcurrentOrderedHashMap::new();
//        map.insert_back(1, "a");
//        assert_eq!(map.get(&1), Some(&"a"));
//        assert_eq!(map.get(&2), None);
//    }
}